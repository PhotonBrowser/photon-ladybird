[package]
name = "photon_core"
version = "0.1.0"
edition = "2024"
publish = false
license = "GPL-3.0-only"

[lib]
crate-type = ["rlib", "staticlib"]

[workspace]

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[lints.clippy]
all = "deny"

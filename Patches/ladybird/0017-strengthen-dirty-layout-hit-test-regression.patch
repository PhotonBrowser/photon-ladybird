--- a/Tests/LibWeb/Text/input/hit_testing/layout-dirtied-before-hit-test.html
+++ b/Tests/LibWeb/Text/input/hit_testing/layout-dirtied-before-hit-test.html
@@ -17,9 +17,11 @@
 <script>
     test(() => {
         const moving = document.getElementById("moving");
+        document.body.getBoundingClientRect();
         println(internals.hitTest(10, 10).node === moving);
 
         moving.style.left = "40px";
+        document.addEventListener("wheel", () => {}, { passive: false });
 
         println(internals.hitTest(50, 10).node === moving);
         println(internals.hitTest(10, 10).node !== moving);

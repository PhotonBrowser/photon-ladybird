# Photon patches

This directory records the small changes Photon must make to Ladybird-owned implementation files. The ordered manifest is `series.toml`; patch files are applied relative to the repository root.

Photon-owned files are normal source files and never belong in this series. See `Documentation/Photon/Patches.md` for the maintenance workflow.
